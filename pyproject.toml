[project]
name = "model-router-budget"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.34.0",
  "httpx>=0.28.0",
  "openai>=1.0.0",
  "psycopg[binary]>=3.2.0",
  "python-dotenv>=1.0.0",
  "pydantic>=2.0.0",
]

[tool.vercel]
entrypoint = "app.main:app"
